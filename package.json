{
  "name": "paperlens",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --test tests/*.test.mjs",
    "serve": "python -m http.server 8080"
  },
  "dependencies": {
    "phaser": "4.2.1"
  }
}
